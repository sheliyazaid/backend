# Society Management ERP — Backend

Node.js + Express + MongoDB API.

## Setup

```powershell
cd society-management-backend
npm install
npm run seed
npm run dev
```

API: **http://localhost:5050**

Login: `admin@society.com` / `admin123`
